/**
 * FUTURO — porras por grupo (pool_slug en la base de datos + selector en App.jsx).
 * Cuando lo implementes: exporta PREDICTION_POOLS y poolLabel, importa en App,
 * filtra/guarda con .eq('pool_slug', …) y upsert onConflict: 'pool_slug,nickname'.
 * SQL de referencia (comentado): predictions-pool-slug.sql en el repositorio.
 */
